该例子解释了如何阻隔某一个线程不输入任何信息

# 为什么线程会输入消息

线程默认data_in_thread设置为main，data_in_slice设置为[-1, ]，每个线程会默认获取输入线程的最新消息

# 如何阻隔某一个线程不输入任何信息

让node的data_in_slice设置为[-1, -1]，这样就不会输入任何信息了

```json
"data_in_slice": [
        -1,
        -1
    ],
```
# 作用

在并行收集 + 聚合的任务时，若不想让某个线程输入任何信息，可以使用这种方法